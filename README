This is a test task for the Yandex company.
The install.py script copies the pre-commit hook to the hooks folder of a destination repository.
The hook itself checks lines of committed files not to exceed maximum length (default - 80 symbols, but can be adjusted in the config file of the repository).